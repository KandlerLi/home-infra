---
- name: Configure home servers
  hosts: home_servers
  become: true
  gather_facts: true

  roles:
    - base
    - storage
    - nfs_server
    - docker
    - deluge
    - nextcloud_aio
    - home_agent
    - open_webui
    - monitoring
    - blocky_ipv6_relay
