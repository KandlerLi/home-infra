---
- name: Configure read-only Nextcloud tools and home agent integration
  hosts: home_servers
  become: true
  gather_facts: true

  roles:
    - role: nextcloud_tools
      nextcloud_tools_enabled: true
    - role: home_agent
      home_agent_enabled: true
      home_agent_nextcloud_tools_enabled: true
